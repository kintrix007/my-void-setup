# system-packages

# Terminal
alacritty

# General System Utilities
bash-completion

# For building from source
git

# Easier per-directory environments, plays well with nix
direnv

# Package managers
flatpak
nix # Apparently there might be a problem with the xbps version?

# Network
NetworkManager network-manager-applet

# Services
socklog-void snooze ntp tlp dbus elogind bluez rtkit polkit PopCorn

# Sound
pipewire alsa-pipewire wireplumber libspa-bluetooth
pamixer pulsemixer pavucontrol

# Fonts
noto-fonts-ttf noto-fonts-cjk noto-fonts-emoji noto-fonts-ttf-extra font-fira-ttf font-firacode liberation-fonts-ttf fonts-roboto-ttf font-adobe-source-code-pro ttf-ubuntu-font-family

# Graphical session
xorg hsetroot xinit lxsession xdg-utils xdg-user-dirs xdg-desktop-portal xsel playerctl

picom xmobar trayer-srg rofi rofi-calc rofi-emoji flameshot dunst zenity

lxappearance arc-theme slim-void-theme breeze-gtk breeze papirus-icon-theme

# Xmonad build dependencies
make gcc stack ncurses-libtinfo-libs ncurses-libtinfo-devel libX11-devel libXft-devel libXinerama-devel libXrandr-devel libXScrnSaver-devel pkg-config

# xbps-src dependencies
curl
